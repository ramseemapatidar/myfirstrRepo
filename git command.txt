git config --list

##set email and username

git config --global user.name "username"
git config --global user.email "email@example.com"

##check username and email

git config user.name
git config user.email

##create file using git

touch file1.text

##git repo

git status
git init

## git tracked

git add

## all file tracked new file or modify

git add .

## put in repo

git commit -m "first commit"

## telling the repo location

git remote add origin https://github.com/ramseemapatidar/myfirstrRepo.git

## git remote location chack

git remote

## push code in branch

git push -u origin master 
or
git push

## check previous commit oneline or filter

git log
git log --oneline
git log --grep="first"
git log --since="30/11/2022"

## git brush zoom in /zoom out

Ctrl+
Ctrl-

## check repo

git remote -v

## create branch (jab bhi hum branch bnate h tb hum simply "git branch aur branchname" supose mujhe
dev name ki branch banani h tb `git branch dev` esse dev name ki branch bn jayegi.
## mujhe kisi commit se branch banani h tb `git log --oneline` ye run krenge fir jis bhi commit pr branch banani h uska 7 digit ka code put karenge `git branch dev2 095f898` es type se use krenge.
## note : direct `git branch branchname` dene se jo bhi last commit hoga vha se branch banegi.

git log --oneline
git branch dev
git branch dev 095f898 

## swich to branch like master to dev

git chackout dev
